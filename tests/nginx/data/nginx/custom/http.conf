server = {
  set $forward_scheme http;
  set $server         "127.0.0.1";
  set $port           7575;

  listen 80

  server_name homarr.example.local
  location / {
    proxy_pass $forward_scheme://$server:$port;
  }
}
